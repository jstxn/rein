# Eval Reports

This directory stores generated REIN harness eval artifacts.

Suggested local flow:

```bash
python3 rein-evals/run.py --demo
python3 rein-evals/score.py
```

The resulting score report is directional v1 evidence, not scientific proof.
